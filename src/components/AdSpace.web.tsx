export const AdSpace = () => (
  <aside
    hidden
    data-wages-ad-slot
    id={`calculator-ad-space`}
    className={`advertisement-space`}
    aria-label={`Advertisement`}
  >
    <span id={`calculator-ad-label`} className={`advertisement-label`}>
      {`Advertisement`}
    </span>
  </aside>
);
